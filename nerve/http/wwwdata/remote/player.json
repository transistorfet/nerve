<%
    player = nerve.get_device('player')

    if player is not None:
	status = player.html_status({})
    else:
	status = { 'error' : "This host does not have a player device" }
    end

    echo(json.dumps(status))
%>
